{
    "name": "${pluginName}",
    "entry": "${modFile}",
    "version": "${modVersion}",
    "type": "native",
    "dependencies": [
        {
            "name": "LegacyMoney"
        }
${permissionDependency}
    ]
}
